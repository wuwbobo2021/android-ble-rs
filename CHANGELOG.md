# Changes

## 0.1.1
* (Breaking change) Fixed `Adapter::default` to be compatible with `bluest` 0.6.x.
* Implement `Send` for `AdapterConfig`.
* Improved `async_util::Excluder`.
* Fixed doc.rs build problem: `aarch64-linux-android` target is used now.

## 0.1.0
* Initial release.
